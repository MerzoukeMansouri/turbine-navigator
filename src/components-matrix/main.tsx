import React from 'react';
import ReactDOM from 'react-dom/client';
import ComponentsMatrix from './ComponentsMatrix';
import './ComponentsMatrix.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ComponentsMatrix />
  </React.StrictMode>
);
